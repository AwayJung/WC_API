<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="wc_api.dao.ItemLikeDAO">

    <!-- 좋아요 추가 -->
    <insert id="insertItemLike">
        INSERT INTO item_like (user_id, item_id, is_liked)
        VALUES (#{userId}, #{itemId}, 1)
    </insert>

    <!-- 좋아요 삭제 -->
    <delete id="deleteItemLike">
        UPDATE item_like
        SET is_liked = 0
        WHERE user_id = #{userId} AND item_id = #{itemId}
    </delete>

    <!-- 좋아요 여부 확인 -->
    <select id="countItemLike" resultType="int">
        SELECT COUNT(*)
        FROM item_like
        WHERE user_id = #{userId} AND item_id = #{itemId} AND is_liked = 1
    </select>

    <!-- 아이템의 전체 좋아요 수 조회 -->
    <select id="countItemLikeByItemId" resultType="long">
        SELECT COUNT(*)
        FROM item_like
        WHERE item_id = #{itemId} AND is_liked = 1
    </select>

    <!-- 사용자가 좋아요한 아이템 ID 목록 조회 -->
    <select id="selectItemIdsByUserId" resultType="long">
        SELECT item_id
        FROM item_like
        WHERE user_id = #{userId} AND is_liked = 1
    </select>

</mapper>